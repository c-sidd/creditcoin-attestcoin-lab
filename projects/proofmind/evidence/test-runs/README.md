# Test Run Evidence

Store sanitized test reports here when they are useful for milestone verification.

Each report should contain:

- commit SHA
- command executed
- environment/network
- test count
- passed/failed/skipped
- failure details
- artifacts produced
- milestone gate

A claim such as `tests pass` must correspond to an actually executed command.
