# Prompt 38 — Ideathon Demo

Build a reliable 3–5 minute demonstration around one compelling ProofMind use case. Prefer the real E2E path when it is available; otherwise use the clearly labeled demo mode without pretending it is live protocol verification.

Prepare a deterministic demo dataset, reset procedure, exact click/command sequence, expected screens, transaction/evidence links, and fallback plan. Explain the innovation as `Attestcoin proof → verified evidence → AI reasoning → deterministic policy → Creditcoin execution`.

Verify the demo from a clean environment and document timing and failure recovery.