# Prompt 10 — Contract Integration

Wire source contract, ASC, and business/decision contract according to the documented interaction model.

## Verify
Check addresses/interfaces, caller permissions, event topics, state transitions, revert propagation, and replay protection. Run an integration test that follows the intended transaction sequence in a controlled environment.

Do not call the system cross-chain complete yet. A local integration test is not evidence of real Attestcoin readability.

Document deployment order, role-grant order, required environment configuration, and observed transaction/event evidence.