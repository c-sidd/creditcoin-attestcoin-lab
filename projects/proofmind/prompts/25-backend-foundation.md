# Prompt 25 — Backend Foundation

Implement the backend boundary described in the project docs. Establish application configuration, routing, health/readiness, authentication boundary if required, service modules, structured errors, persistence abstraction, and API versioning.

Keep blockchain/AI logic in dedicated services rather than controllers. Validate all external input. Add unit/API tests and local setup instructions. Do not expose private keys or internal proof payloads unnecessarily.