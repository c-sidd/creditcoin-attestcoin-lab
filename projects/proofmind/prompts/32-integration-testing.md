# Prompt 32 — Full Integration Testing

Connect source contract, worker, proof-builder client, ASC, decision contract, AI service, backend, and frontend through controlled integration environments.

Run the documented lifecycle and assert durable state at every boundary. Include failure injection for RPC downtime, proof-builder failure, invalid proof, ASC revert, AI rejection, backend failure, and frontend/API errors.

Record which dependencies are real and which are mocked. Do not call the project cross-chain complete until real Attestcoin verification evidence exists.