# Prompt 31 — Wallet Flow

Implement only the wallet interaction required by the documented user flow. Clearly distinguish user-signed source-chain actions from worker/backend-submitted actions.

Verify correct network, chain ID/address format, transaction parameters, rejection handling, pending/confirmed states, and UI feedback. Never request or store private keys/seed phrases. Test disconnected wallet, wrong network, rejected signature, pending transaction, and successful confirmation.