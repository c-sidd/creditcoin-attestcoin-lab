# Prompt 28 — Frontend Foundation

Build the documented frontend shell without inventing product behavior. Establish routing, API client, typed models, loading/error states, wallet integration boundary, and reusable UI components.

Keep API/network configuration environment-driven. Never place private keys in the browser. Add a clear development mode indicator where mock data is used.

Verify build, route loading, API failure behavior, and type checking. Document local frontend setup.